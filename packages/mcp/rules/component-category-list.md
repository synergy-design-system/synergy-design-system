When responding with component categories:

- Keep category ids unchanged so they can be reused as `category` input in `component-list`.
- Some components may appear in multiple categories, based on usage.
- Do not fabricate categories that are not present in metadata.
