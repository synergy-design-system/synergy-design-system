// Export functions from the localize lib so we have one central place to import them from
export { registerTranslation } from './localize/core.js';
export { LocalizeController } from './localize/controller.js';
export type { Translation } from './localize/types.js';
