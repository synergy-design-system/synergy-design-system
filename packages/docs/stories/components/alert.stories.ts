// ---------------------------------------------------------------------
// 🔒 AUTOGENERATED BY VENDORISM
// Removing this comment will prevent it from being managed by it.
// ---------------------------------------------------------------------

/* eslint-disable */

/* eslint-disable import/no-relative-packages */

import '../../../components/src/components/alert/alert';
import type { Meta, StoryObj } from '@storybook/web-components';
import { html } from 'lit';
import { storybookDefaults, storybookHelpers, storybookTemplate, generateStoryDescription } from '../../src/helpers/component.js';
const { args, argTypes } = storybookDefaults('syn-alert');
const { overrideArgs } = storybookHelpers('syn-alert');
const { generateTemplate } = storybookTemplate('syn-alert');

const meta: Meta = {
  component: 'alert',
  args: overrideArgs([ { type: 'slot', value: 'This is a standard alert. You can customize its content and even the icon.', name: 'default' }, { type: 'attribute', value: true, name: 'open' }, { type: 'slot', value: '<syn-icon slot="icon" name="info-circle"></syn-icon>', name: 'icon' } ], args),
  argTypes,
  title: 'Components/syn-alert',
  parameters: {
    docs: {
      description: {
        component: generateStoryDescription('alert', 'default'),
      },
    }
  }
};
export default meta;

type Story = StoryObj;

export const Default = {
  render: (args: any) => {
    return generateTemplate({ args });
  },
  parameters: {
    docs: {
      description: {
        story: generateStoryDescription('alert', 'default'),
      }
    }
  }
} as Story;


/**
 * Set the variant attribute to change the alert's variant.
 */
export const Variants: Story = {
  render: () => html`<syn-alert variant="primary" open>
  <syn-icon slot="icon" name="info-circle"></syn-icon>
  <strong>This is super informative</strong><br />
  You can tell by how pretty the alert is.
</syn-alert>

<br />

<syn-alert variant="success" open>
  <syn-icon slot="icon" name="check2-circle"></syn-icon>
  <strong>Your changes have been saved</strong><br />
  You can safely exit the app now.
</syn-alert>

<br />

<syn-alert variant="neutral" open>
  <syn-icon slot="icon" name="gear"></syn-icon>
  <strong>Your settings have been updated</strong><br />
  Settings will take effect on next login.
</syn-alert>

<br />

<syn-alert variant="warning" open>
  <syn-icon slot="icon" name="exclamation-triangle"></syn-icon>
  <strong>Your session has ended</strong><br />
  Please login again to continue.
</syn-alert>

<br />

<syn-alert variant="danger" open>
  <syn-icon slot="icon" name="exclamation-octagon"></syn-icon>
  <strong>Your account has been deleted</strong><br />
  We're very sorry to see you go!
</syn-alert>`,
};

/**
 * Add the closable attribute to show a close button that will hide the alert.
 */
export const Closable: Story = {
  render: () => html`<syn-alert variant="primary" open closable class="alert-closable">
  <syn-icon slot="icon" name="info-circle"></syn-icon>
  You can close this alert any time!
</syn-alert>

<script type="module">
  const alert = document.querySelector('.alert-closable');
  alert.addEventListener('syn-after-hide', () => {
    setTimeout(() => (alert.open = true), 2000);
  });
</script>`,
};

/**
 * Icons are optional. Simply omit the icon slot if you don't want them.
 */
export const WithoutIcons: Story = {
  render: () => html`<syn-alert variant="primary" open> Nothing fancy here, just a simple alert. </syn-alert>`,
};

/**
 * Set the duration attribute to automatically hide an alert after a period of time. This is useful for alerts that don't require acknowledgement.
 */
export const Duration: Story = {
  render: () => html`<div class="alert-duration">
  <syn-button variant="primary">Show Alert</syn-button>

  <syn-alert variant="primary" duration="3000" closable>
    <syn-icon slot="icon" name="info-circle"></syn-icon>
    This alert will automatically hide itself after three seconds, unless you interact with it.
  </syn-alert>
</div>

<script type="module">
  const container = document.querySelector('.alert-duration');
  const button = container.querySelector('syn-button');
  const alert = container.querySelector('syn-alert');

  button.addEventListener('click', () => alert.show());
</script>

<style>
  .alert-duration syn-alert {
    margin-top: var(--syn-spacing-medium);
  }
</style>`,
};

/**
 * To display an alert as a toast notification, or "toast", create the alert and call its toast() method. This will move the alert out of its position in the DOM and into  where it will be shown. Once dismissed, it will be removed from the DOM completely. To reuse a toast, store a reference to it and call toast() again later on.You should always use the closable attribute so users can dismiss the notification. It's also common to set a reasonable duration when the notification doesn't require acknowledgement.
 */
export const ToastNotifications: Story = {
  render: () => html`<div class="alert-toast">
  <syn-button variant="primary">Primary</syn-button>
  <syn-button variant="success">Success</syn-button>
  <syn-button variant="neutral">Neutral</syn-button>
  <syn-button variant="warning">Warning</syn-button>
  <syn-button variant="danger">Danger</syn-button>

  <syn-alert variant="primary" duration="3000" closable>
    <syn-icon slot="icon" name="info-circle"></syn-icon>
    <strong>This is super informative</strong><br />
    You can tell by how pretty the alert is.
  </syn-alert>

  <syn-alert variant="success" duration="3000" closable>
    <syn-icon slot="icon" name="check2-circle"></syn-icon>
    <strong>Your changes have been saved</strong><br />
    You can safely exit the app now.
  </syn-alert>

  <syn-alert variant="neutral" duration="3000" closable>
    <syn-icon slot="icon" name="gear"></syn-icon>
    <strong>Your settings have been updated</strong><br />
    Settings will take effect on next login.
  </syn-alert>

  <syn-alert variant="warning" duration="3000" closable>
    <syn-icon slot="icon" name="exclamation-triangle"></syn-icon>
    <strong>Your session has ended</strong><br />
    Please login again to continue.
  </syn-alert>

  <syn-alert variant="danger" duration="3000" closable>
    <syn-icon slot="icon" name="exclamation-octagon"></syn-icon>
    <strong>Your account has been deleted</strong><br />
    We're very sorry to see you go!
  </syn-alert>
</div>

<script type="module">
  const container = document.querySelector('.alert-toast');

  ['primary', 'success', 'neutral', 'warning', 'danger'].map(variant => {
    const button = container.querySelector(\`syn-button[variant="\${variant}"]\`);
    const alert = container.querySelector(\`syn-alert[variant="\${variant}"]\`);

    button.addEventListener('click', () => alert.toast());
  });
</script>`,
};

/**
 * For convenience, you can create a utility that emits toast notifications with a function call rather than composing them in your HTML. To do this, generate the alert with JavaScript, append it to the body, and call the toast() method as shown in the example below.
 */
export const CreatingToastsImperatively: Story = {
  render: () => html`<div class="alert-toast-wrapper">
  <syn-button variant="primary">Create Toast</syn-button>
</div>

<script type="module">
  const container = document.querySelector('.alert-toast-wrapper');
  const button = container.querySelector('syn-button');
  let count = 0;

  // Always escape HTML for text arguments!
  function escapeHtml(html) {
    const div = document.createElement('div');
    div.textContent = html;
    return div.innerHTML;
  }

  // Custom function to emit toast notifications
  function notify(message, variant = 'primary', icon = 'info-circle', duration = 3000) {
    const alert = Object.assign(document.createElement('syn-alert'), {
      variant,
      closable: true,
      duration: duration,
      innerHTML: \`
        <syn-icon name="\${icon}" slot="icon"></syn-icon>
        \${escapeHtml(message)}
      \`
    });

    document.body.append(alert);
    return alert.toast();
  }

  button.addEventListener('click', () => {
    notify(\`This is custom toast #\${++count}\`);
  });
</script>`,
};
