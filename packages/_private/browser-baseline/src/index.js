/**
 * Do something
 * @param {number} a 
 * @param {number} b 
 * @returns {number} The added numbers
 */
export const something = (a, b) => a + b;

export const somethingElse = (a, b) => {
  return a * b;
};
