// ---------------------------------------------------------------------
// 🔒 AUTOGENERATED BY VENDORISM
// Removing this comment will prevent it from being managed by it.
// ---------------------------------------------------------------------

/* eslint-disable */

/* eslint-disable import/no-relative-packages */

import '../../../components/src/components/switch/switch';
import type { Meta, StoryObj } from '@storybook/web-components';
import { html } from 'lit';
import docsTokens from '../../../tokens/src/figma-tokens/_docs.json';
import { storybookDefaults, storybookHelpers, storybookTemplate } from '../../src/helpers/component.js';
const { args, argTypes } = storybookDefaults('syn-switch');
const { overrideArgs } = storybookHelpers('syn-switch');
const { generateTemplate } = storybookTemplate('syn-switch');

const meta: Meta = {
  component: 'switch',
  args,
  argTypes,
  title: 'Components/syn-switch',
  parameters: {
    docs: {
      description: {
        component: docsTokens?.components?.['switch']?.default?.description?.value ?? 'No Description',
      },
    }
  }
};
export default meta;

type Story = StoryObj;

export const Default = {
  render: (args: any) => {
    return generateTemplate({ args });
  },
  parameters: {
    docs: {
      description: {
        story: docsTokens?.components?.['switch']?.default?.description?.value ?? 'No Description',
      }
    }
  }
} as Story;


/**
 * Use the checked attribute to activate the switch.
 */
export const Checked: Story = {
  render: () => html`<syn-switch checked>Checked</syn-switch>`,
};

/**
 * Use the disabled attribute to disable the switch.
 */
export const Disabled: Story = {
  render: () => html`<syn-switch disabled>Disabled</syn-switch>`,
};

/**
 * Use the size attribute to change a switch's size.
 */
export const Sizes: Story = {
  render: () => html`<syn-switch size="small">Small</syn-switch>
<br />
<syn-switch size="medium">Medium</syn-switch>
<br />
<syn-switch size="large">Large</syn-switch>`,
};

/**
 * Use the available custom properties to change how the switch is styled.
 */
export const CustomStyles: Story = {
  render: () => html`<syn-switch style="--width: 80px; --height: 40px; --thumb-size: 36px;">Really big</syn-switch>`,
};
