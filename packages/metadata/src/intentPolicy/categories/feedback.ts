import type { IntentCategory } from '../types.js';

export const feedbackCategory: IntentCategory = {
  description: 'Contextual user feedback such as inline or toast notifications.',
  id: 'feedback',
};
