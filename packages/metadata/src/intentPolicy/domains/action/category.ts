import type { IntentCategory } from '../../types.js';

export const actionCategory: IntentCategory = {
  description: 'Contextual actions such as primary, submit, reset, or navigation actions.',
  id: 'action',
};
