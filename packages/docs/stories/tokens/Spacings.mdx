import { Markdown, Meta } from "@storybook/blocks";
import { CopyToClipBoard } from '../../src/shared-components/Clipboard.tsx';
import { CopyRawCssPropValue } from '../../src/shared-components/RawColorValue.tsx';
import docsTokens from '../../../tokens/src/figma-tokens/_docs.json';
import { Spacing, MultiLineDescription } from '../../src/shared-components/TokenHelpers.tsx';

import {
  getCSSToken,
  getSASSToken,
  getSpacings,
} from '../../src/helpers';

<Meta title="Tokens/Spacings" />

# {docsTokens?.tokens?.spacing?.title.value}

<MultiLineDescription value={docsTokens?.tokens?.spacing?.description?.value} />

{['synergy', 'brand25'].map(theme => (
  <div key={theme}>
    <Markdown>{`## Spacing (${theme})`}</Markdown>
    <table className={theme === 'synergy' ? `syn-theme-light` : `syn-theme-brand25-light`}>
      <tbody>
        <>
          <tr>
            <th>{docsTokens?.tokens?.spacing?.title.value}</th>
            <th>Raw Value</th>
            <th>CSS Token</th>
            <th>SCSS Token</th>
            <th>JS Token</th>
          </tr>
          {Object.entries(getSpacings(true)).map(([token, value]) => (
            <tr key={`${token}-row`}>
              <td>
                <Spacing className={token.toLowerCase()} borderRadius="var(--syn-border-radius-none)" value={value} />
              </td>
              <td>
                <CopyRawCssPropValue value={token.toLowerCase()} cssProperty="width" />
              </td>
              <td>
                <CopyToClipBoard value={getCSSToken(token)}>
                  {getCSSToken(token)}
                </CopyToClipBoard>
              </td>
              <td>
                <CopyToClipBoard value={getSASSToken(token)}>
                  {getSASSToken(token)}
                </CopyToClipBoard>
              </td>
              <td>
                <CopyToClipBoard value={token}>
                  {token}
                </CopyToClipBoard>
              </td>
            </tr>
          ))}
        </>
      </tbody>
    </table>
  </div>
))}
