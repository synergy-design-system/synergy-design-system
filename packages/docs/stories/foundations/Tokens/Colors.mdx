import { Meta } from "@storybook/blocks";
import * as tokens from '@sick-design-system/design-tokens';

<Meta title="Foundations/Design Tokens/Colors" />

# Colors

<div className="color-swatches">
  {
    Object
      .entries(tokens)
      .filter(([token]) => token.toLowerCase().startsWith('sdscolor'))
      .map(([token, value]) => (
        <div
          className="color-swatch-wrapper"
          title={token}
          key={token}
        >
          <div className="color-swatch" style={{ backgroundColor: value }} />
          <div className="color-swatch-label">
            {token}
          </div>
        </div>
      ))
  }
</div>

<style>
{`
  .color-swatches {
    display: grid;
    grid-template-columns: repeat(5, auto);
    gap: 2rem;
    justify-items: center;
  }

  .color-swatch-wrapper {
    justify-content: center;
    flex-direction: column;
    display: flex;
  }

  .color-swatch {
    border-radius: 100%;
    display: flex;
    height: 150px;
    width: 150px;
  }

  .color-swatch-label {
    background: #fff;
    color: #000;
    font-size: 10px;
    margin-top: 1rem;
    text-align: center;
  }
`}
</style>
