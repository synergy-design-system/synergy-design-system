import { Meta } from "@storybook/blocks";
import { CopyToClipBoard } from '../../src/Clipboard.tsx';
import { CopyRawCssPropValue } from '../../src/RawColorValue.tsx';
import docsTokens from '../../../tokens/src/figma-tokens/_docs.json';
import { ColorSwatch } from '../../src/ColorSwatch.tsx';

import {
  getCSSToken,
  getSpacings,
} from '../../src/helpers';

<Meta title="Tokens/Spacings" />

# {docsTokens?.tokens?.spacing?.title.value}

<p>
  {docsTokens?.tokens?.spacing?.description?.value}
</p>

  <div key={'syn-spacing'}>
    <table>
      <tbody>
          <>
            <tr>
              <th>{docsTokens?.tokens?.spacing?.title.value}</th>
              <th>Raw Value</th>
              <th>CSS Token</th>
            </tr>
            {Object.entries(getSpacings(true)).map(([token, value]) => (
              <tr key={`${token}-row`}>
                <td>
                  <ColorSwatch className={ token.toLowerCase() } width={value} height={value} borderRadius="var(--syn-border-radius-none)"/>
                </td>
                <td>
                  <CopyRawCssPropValue value={token.toLowerCase()} cssProperty="width" />
                </td>
                <td>
                  <CopyToClipBoard value={getCSSToken(token)}>
                    {getCSSToken(token)}
                  </CopyToClipBoard>
                </td>
              </tr>
            ))}
          </>
      </tbody>
    </table>
  </div>
