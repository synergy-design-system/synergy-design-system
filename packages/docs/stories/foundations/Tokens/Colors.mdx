import { Meta } from "@storybook/blocks";
import * as tokens from '@sick-design-system/design-tokens';
import { paramCase } from 'change-case';
import {
  getAccentColorPalette,
  getAddonsColorPalette,
  getPrimaryColorPalette,
  getSecondaryColorPalette
} from '../../../src/helpers';

<Meta title="Foundations/Design Tokens/Colors" />

# Colors

<table>
  <thead>
    <tr>
      <th colSpan="2">Color</th>
      <th>CSS Token</th>
      <th>SCSS Token</th>
      <th>JS Token</th>
    </tr>
  </thead>
  <tbody>
    
    <tr>
      <th colSpan="5">Primary Colors</th>
    </tr>
    {Object
      .entries(getPrimaryColorPalette())
      .map(([token, value]) => (
        <tr key={token}>
          <td>
            <div className="color-swatch" style={{ backgroundColor: value }} />
          </td>
          <td>{value}</td>
          <td>
            --
            {paramCase(token, {
              splitRegexp: /([a-z])([A-Z0-9])/g,
            })}
          </td>
          <td>
            ${paramCase(token, {
              splitRegexp: /([a-z])([A-Z0-9])/g,
            })}
          </td>
          <td>{token}</td>
        </tr>
      ))
    }

    <tr>
      <th colSpan="5">Secondary Colors</th>
    </tr>
    {Object
      .entries(getSecondaryColorPalette())
      .map(([token, value]) => (
        <tr key={token}>
          <td>
            <div className="color-swatch" style={{ backgroundColor: value }} />
          </td>
          <td>{value}</td>
          <td>
            --
            {paramCase(token, {
              splitRegexp: /([a-z])([A-Z0-9])/g,
            })}
          </td>
          <td>
            ${paramCase(token, {
              splitRegexp: /([a-z])([A-Z0-9])/g,
            })}
          </td>
          <td>{token}</td>
        </tr>
      ))
    }

    <tr>
      <th colSpan="5">Accent Colors</th>
    </tr>
    {Object
      .entries(getAccentColorPalette())
      .map(([token, value]) => (
        <tr key={token}>
          <td>
            <div className="color-swatch" style={{ backgroundColor: value }} />
          </td>
          <td>{value}</td>
          <td>
            --
            {paramCase(token, {
              splitRegexp: /([a-z])([A-Z0-9])/g,
            })}
          </td>
          <td>
            ${paramCase(token, {
              splitRegexp: /([a-z])([A-Z0-9])/g,
            })}
          </td>
          <td>{token}</td>
        </tr>
      ))
    }

    <tr>
      <th colSpan="5">Additional Colors</th>
    </tr>
    {Object
      .entries(getAddonsColorPalette())
      .map(([token, value]) => (
        <tr key={token}>
          <td>
            <div className="color-swatch" style={{ backgroundColor: value }} />
          </td>
          <td>{value}</td>
          <td>
            --
            {paramCase(token, {
              splitRegexp: /([a-z])([A-Z0-9])/g,
            })}
          </td>
          <td>
            ${paramCase(token, {
              splitRegexp: /([a-z])([A-Z0-9])/g,
            })}
          </td>
          <td>{token}</td>
        </tr>
      ))
    }
  </tbody>
</table>

<style>
{`
  .color-swatch {
    align-items: center;
    border-radius: 2px;
    box-shadow: 0 0 3px rgba(0, 0, 0, .2);
    display: flex;
    height: 40px;
    justify-content: center;
    width: 40px;
  }
`}
</style>
