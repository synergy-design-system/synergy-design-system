{
  "name": "@synergy-design-system/e2e-demo-test",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "pnpm test",
    "test": "pnpm run /^prepare..*/ && pnpm exec playwright test --grep-invert \"Platform contracts\"",
    "test.ui": "pnpm run test --ui",
    "prepare.angular": "pnpm run -C ../angular-demo _build",
    "test.angular": "pnpm run prepare.angular && TEST_FRAMEWORK=angular playwright test --grep-invert \"Platform contracts\"",
    "prepare.platform": "pnpm run -C ../platform-contract-demo _build",
    "test.platform": "pnpm run prepare.platform && TEST_FRAMEWORK=platform playwright test src/PlatformContracts",
    "prepare.react": "pnpm run -C ../react-demo _build",
    "test.react": "pnpm prepare.react && TEST_FRAMEWORK=react playwright test --grep-invert \"Platform contracts\"",
    "prepare.vanilla": "pnpm run -C ../vanilla-demo _build",
    "test.vanilla": "pnpm run prepare.vanilla && TEST_FRAMEWORK=vanilla playwright test --grep-invert \"Platform contracts\"",
    "prepare.vue": "pnpm run -C ../vue-demo _build",
    "test.vue": "pnpm run prepare.vue && TEST_FRAMEWORK=vue playwright test --grep-invert \"Platform contracts\"",
    "lint:js": "eslint src",
    "lint": "pnpm run /^lint:.*/"
  },
  "devDependencies": {
    "@synergy-design-system/eslint-config-syn": "workspace:^",
    "@types/node": "^24.13.3",
    "eslint": "^9.39.5",
    "serve": "^14.2.6",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@playwright/test": "^1.62.1",
    "@synergy-design-system/components": "workspace:^",
    "@synergy-design-system/tokens": "workspace:^"
  }
}
