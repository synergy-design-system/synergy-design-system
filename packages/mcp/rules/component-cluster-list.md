When responding with component clusters:
- Prefer compact JSON output.
- Keep cluster ids unchanged so they can be reused as `cluster` input in `component-list`.
- Do not fabricate clusters that are not present in metadata.
