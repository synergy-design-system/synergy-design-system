# Compatibility Notice

This tool is deprecated and remains available for backwards compatibility.

For new requests, use `intent-discover`. It supports both category discovery and listing intents within a category.

Do not use category IDs as intent IDs. Obtain exact registered intent IDs from `intent-discover` and use them verbatim.
